import{_ as t,c as a,o,a5 as s,fb as i,fc as n,fd as r,fe as l,ff as c,fg as p,fh as d,fi as m,fj as g,fk as b,fl as h,fm as u,fn as f,fo as _,fp as w,fq as T,fr as y}from"./chunks/framework.Rv4d7cGz.js";const M=JSON.parse('{"title":"Tables","description":"","frontmatter":{},"headers":[],"relativePath":"docs/guide-table.md","filePath":"docs/guide-table.md","lastUpdated":1763430339000}'),k={name:"docs/guide-table.md"};function A(q,e,I,P,x,v){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="tables" tabindex="-1">Tables <a class="header-anchor" href="#tables" aria-label="Permalink to “Tables”">​</a></h1><p>In Liii STEM, matrices, aligned formula , etc., can all be regarded as &quot;tables&quot; ( Inserting a table by pressing <code>Alt + t</code> in <em>math mode</em>, it can be transformed into matrices, determinants, etc. by <code>Tab</code><a href="https://liiistem.com/docs/guide-equation.html" target="_blank" rel="noreferrer"> cycling</a>).</p><p>TableInsertion and Reference Methods are detailed in <a href="https://liiistem.cn/docs/guide-lines.html" target="_blank" rel="noreferrer">Cross-Referencing</a>.</p><p>You learn further in mastering the usage of tables, <strong>matrix</strong> and <strong>piecewise functions</strong> in this guide.</p><h2 id="_1-types-of-tables" tabindex="-1">1. Types of Tables <a class="header-anchor" href="#_1-types-of-tables" aria-label="Permalink to “1. Types of Tables”">​</a></h2><p>Ordinary tables, a type of table without numbering and captions. It is categorized by its width, alignment, and the presence of labels and borders.</p><p><img src="'+i+'" alt=""></p><p>Numbered tables, a type of table <strong>with numbering</strong> and <strong>captions.</strong> A big table<strong>occupies a single</strong>line while a small table doesn&#39;t.</p><p><img src="'+n+'" alt=""></p><p><img src="'+r+'" alt=""></p><h2 id="_2-table-editing" tabindex="-1">2. Table Editing <a class="header-anchor" href="#_2-table-editing" aria-label="Permalink to “2. Table Editing”">​</a></h2><p><strong>Insert Table</strong>: Click the <code>insert a table</code> button in the <em>mode toolbar</em> (as shown below) and select the table type.</p><p><img src="'+l+'" alt=""></p><p><strong>Add/Del Rows &amp; Cols:</strong> A new table starts as a single empty cell. When the table is selected, its controls will appear in the <em>focus (current environment) toolbar</em>.</p><p><img src="'+c+'" alt=""></p><p>From left to right, are: insert row above, insert column to the left, insert column to the right, and insert row below; the last four are the corresponding deletion operations.</p><blockquote><p>Tip： Use <a href="https://liiistem.com/docs/guide-magic-paste.html" target="_blank" rel="noreferrer">Magic Paste</a> on any AI-generated table to get a ready-to-go version in one click. <img src="'+p+'" alt=""><img src="'+d+'" alt=""></p></blockquote><h2 id="_3-table-basics" tabindex="-1">3. Table Basics <a class="header-anchor" href="#_3-table-basics" aria-label="Permalink to “3. Table Basics”">​</a></h2><p>Click the →<code>Table properties</code> in the <em>focus (current environment) toolbar</em> to open<strong>Table Properties.</strong></p><p><img src="'+m+'" alt=""></p><p>Or <code>right-click</code> after selecting the table to bring up the function bar .</p><p><img src="'+g+'" alt=""><img src="'+b+'" alt=""></p><ul><li><strong>Set Table Size</strong></li></ul><p>You can set the number of rows and columns in four ways.</p><p>**a.**Settle from the <em>focus (current environment) toolbar</em> .</p><p><strong>b.</strong> Set &quot;Rows&quot; and &quot;Columns&quot; in the Table Properties window.</p><p>**c.**Right click to bring up the function bar, then set it in <code>Table</code> → <code>Size</code>.</p><p><strong>d.</strong> Use the shortcut <code>Alt</code>+<code>Arrows</code>.</p><p>Additionally, you can also set <strong>min/max</strong> row and column counts for the table, which helps in building custom table macros.</p><blockquote><p>Note: Any row/column property set outside the permitted min/max range will be ignored, and the current property will remain unchanged.</p></blockquote><ul><li><strong>Width and Height Adjustment</strong></li></ul><p>You can set the table&#39;s overall width and height in Table Properties or the function bar. Two common sizing strategies are:</p><p><strong>1.</strong> Wide Table: In the Function Bar <code>Width</code>→ <code>Paragraph</code>, it will fill up the <strong>Paragraph</strong> after adjustment.</p><p><strong>2.</strong> Inline table: In the function bar <code>Width</code>→ <code>Automatic</code>, the size will be <strong>automatically determined by the content</strong>after adjustment.</p><p><img src="'+h+'" alt=""><img src="'+u+'" alt=""></p><ul><li><strong>Split Page</strong></li></ul><p>For long tables presented across pages, you can click <code>Enable page breaking</code> in <strong>Table Properties</strong> to enable page breaking.</p><p><img src="'+f+'" alt=""></p><blockquote><p>Note: If the cell is a block, it is recommended to set the <strong>line/paragraph spacing</strong> of this paragraph to <code>0</code> to avoid extra whitespace at the page break.</p></blockquote><h2 id="_4-applications-in-mathematics" tabindex="-1">4. Applications in Mathematics <a class="header-anchor" href="#_4-applications-in-mathematics" aria-label="Permalink to “4. Applications in Mathematics”">​</a></h2><p>Matrices are presented in the form of tables in Liii STEM: you can add or delete rows and columns, set alignment and dimensions just like editing a table, and then select the outer delimiters (parentheses, square brackets, vertical bars, etc.).</p><p>Thus, most table editing methods are equally applicable to matrices.</p><ul><li><strong>Matrix</strong></li></ul><p>Click <code>Insert math</code>→<code>Single line/Inline Formula</code>.</p><p><img src="'+_+'" alt=""></p><p>In <em>math mode</em>, click on the <em>focus (current environment) toolbar</em> <code>Insert table</code> → <code>Matrix</code>. Then we have a <code>1x1</code> empty matrix in the formula line.</p><p><img src="'+w+'" alt=""></p><p>Alternatively, use shortcuts:</p><p>In <em>math mode</em>, press <code>Alt + T</code> (generates a table) + <code>Tab</code> (turn into matrix), and turn it into determinants, Bmatrices, piecewise functions, stacked subscripts by <a href="https://liiistem.com/docs/guide-equation.html" target="_blank" rel="noreferrer">Tab cycling</a>.</p><p>Rows and columns can also be added via <code>Alt + Arrows.</code></p><ul><li><strong>Piecewise Function</strong></li></ul><p>A piecewise function is essentially a table with a left-side brace. We can edit it in the same way as in a table:</p><p><strong>a.</strong> In math mode, use the <code>\\choice</code> command to insert a brace and a single-column table. Or use the shortcut <code>Alt</code>+<code>t</code> to generate a table and turn it into a piecewise function by Tab cycling.</p><p><strong>b.</strong> Use <code>Alt + Arrows</code> to add columns or rows.</p><p>**c.**Manually input the function, equal signs, and other components.</p><p><img src="'+T+'" alt=""><img src="'+y+'" alt=""></p>',56)])])}const C=t(k,[["render",A]]);export{M as __pageData,C as default};
