import{_ as i,c as t,o as a,a5 as n,cJ as o,cK as r,cL as s,cM as d,cN as g,cO as l,cP as c,cQ as m}from"./chunks/framework.Rv4d7cGz.js";const w=JSON.parse('{"title":"Advanced Figure and Image Editing","description":"","frontmatter":{},"headers":[],"relativePath":"docs/guide-image.md","filePath":"docs/guide-image.md","lastUpdated":1763430339000}'),p={name:"docs/guide-image.md"};function u(h,e,f,_,b,I){return a(),t("div",null,[...e[0]||(e[0]=[n('<h1 id="advanced-figure-and-image-editing" tabindex="-1">Advanced Figure and Image Editing <a class="header-anchor" href="#advanced-figure-and-image-editing" aria-label="Permalink to “Advanced Figure and Image Editing”">​</a></h1><p><strong>Figures</strong> are commonly used environments in Liii STEM&#39;s <em>text mode</em>. For insertion and referencing, see <a href="https://liiistem.com/docs/guide-lines.html" target="_blank" rel="noreferrer">Cross-Referencing</a>. This guide covers all figure-related operations.</p><h2 id="_1-figure-vs-image" tabindex="-1">1. Figure vs. Image <a class="header-anchor" href="#_1-figure-vs-image" aria-label="Permalink to “1. Figure vs. Image”">​</a></h2><p>In Liii STEM:</p><ul><li>A figure (a numbered environment) is defined as an image with a caption.</li><li>An image is defined as a standalone picture without a caption.</li></ul><p>These concepts enable the following distinct operations.</p><h2 id="_2-inserting-figures-and-images" tabindex="-1">2. Inserting Figures and Images <a class="header-anchor" href="#_2-inserting-figures-and-images" aria-label="Permalink to “2. Inserting Figures and Images”">​</a></h2><ul><li><strong>Inserting a Figure</strong></li></ul><p>Click <code>Insert a picture</code> button in the <code>focus (current environment) toolbar</code> →<code>Small/Big figure</code>.</p><p><img src="'+o+'" alt=""></p><blockquote><p>After inserting a Figure, paste or insert your needed image inside the Figure environment.</p></blockquote><p><img src="'+r+'" alt=""></p><ul><li><strong>Inserting an Image</strong></li></ul><p>To insert only an image.</p><p><img src="'+s+'" alt=""></p><p>Two methods are available for inserting an image:</p><p><strong>a.</strong> Insert the image directly into the document (similar to inserting images in Word). <em>Mode Toolbar</em> → <code>Insert Image</code> button → <code>Insert Image...</code></p><p><img src="'+d+'" alt=""></p><p><strong>b.</strong> Inserts only a link to the image (similar to inserting images in LaTeX).</p><blockquote><p>Advantages: Smaller document size, documents can share the same image, images can be updated independently. Disadvantages: Document is not containing the image; sharing the document requires separately sending all linked images.</p></blockquote><p><em>Mode toolbar</em> → <code>Insert Image</code> button → <code>Link Image...</code></p><p><img src="'+g+'" alt=""></p><ul><li>Use the <code>↑</code> button in the <em>focus (current environment) toolbar</em> to convert a linked image to an inserted one.</li></ul><p><img src="'+l+'" alt=""></p><h2 id="_3-image-properties" tabindex="-1">3. Image Properties <a class="header-anchor" href="#_3-image-properties" aria-label="Permalink to “3. Image Properties”">​</a></h2><p>By default, images are displayed at their original size. You can adjust the width, height, and position of an image in the <em>focus (current environment) toolbar</em>.</p><ul><li><strong>Resizing</strong></li></ul><p>If the width or height is set only, the image will be scaled proportionally.</p><p>For example, setting the width to <code>1par</code> will make the image span the full paragraph, with the height adjusted automatically.</p><blockquote><p>For regular images, we recommend setting the width to <code>0.5par</code>.</p></blockquote><p><img src="'+c+'" alt=""></p><p>Use <code>w</code> and <code>h</code> to represent the default width and height of the image.</p><p>For example, setting the width to <code>2w</code> and height to <code>2h</code> will display the image at twice its default size.</p><ul><li><strong>Repositioning</strong></li></ul><p>Use <code>-</code> to shift the image leftward or downward. Enter a percentage value (e.g., <code>50%</code>) to specify the movement distance.</p><p>Example: Setting the X to <code>50%</code> centers the image.</p><blockquote><p>Note: Setting of the y only works in Figure environments. Images do not allow vertical adjustment. The Big Figure environment automatically centers images.</p></blockquote><p><img src="'+m+'" alt=""></p>',38)])])}const k=i(p,[["render",u]]);export{w as __pageData,k as default};
